import React from 'react';
import AgePyramid from './AgePyramid';
import { Box } from '@mui/material';

const AgePyramidPage: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <AgePyramid />
    </Box>
  );
};

export default AgePyramidPage;
